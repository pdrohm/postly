import { StyleSheet } from "react-native";

export const postCardImageStyles = StyleSheet.create({
  image: {
    width: "100%",
    height: 240,
    borderRadius: 12,
    backgroundColor: "#f2f2f2",
    marginBottom: 8,
  },
}); 